services:
  - type: web
    name: chungai-home
    env: static
    buildCommand: chmod +x build.sh && ./build.sh
    staticPublishPath: .
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false

